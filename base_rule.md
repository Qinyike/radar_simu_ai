# 提示词：构建可扩展、可维护的雷达（或信号处理）仿真框架的架构指南
核心目标：设计一个代码架构，使得新增功能（如新算法、新波形）像“插入模块”一样简单，而非“修改系统”，从而实现代码的线性、可管理增长。
一、顶层设计：分层架构与单向依赖
强制分层结构：将系统垂直划分为界限分明的层次。一个参考模型如下：
入口/调度层：负责解析配置，注册并调用具体的仿真模块。
可视化/输出层：负责将统一格式的处理结果绘制成图或输出报告。它只关心输入数据的格式，不关心其来源。
信号处理/算法层：接收统一的原始数据格式，执行各类算法（如FFT、CFAR、DOA估计），并输出统一格式的结果。它不关心数据是如何仿真出来的。
波形生成/仿真层：包含所有具体的仿真模块（如FMCW、LFM、PMCW）。每个模块接收参数，输出完全统一格式的原始仿真数据。模块之间互相独立，互不知晓。
数据定义/契约层：定义层与层之间传递的核心数据结构（接口契约），这是架构的基石。
外部引擎/工具层：封装对第三方库（如numpy, radarsimpy, matplotlib）的调用，实现细节隔离。
铁律：单向数据流：依赖关系必须是单向的，只能从上层调用下层。例如，可视化层可以调用处理层，但处理层绝不能反向调用可视化层。严禁任何形式的循环依赖或跨层调用。
二、架构骨骼：定义清晰的接口契约
必须定义并严格遵守两个核心数据结构作为层间通信的“标准语言”：
仿真结果契约 (SimResult)：
目的：仿真层​ 与 处理层​ 之间的契约。
内容：必须包含所有处理算法所需的原始数据与关键参数。一个典型示例包括：
name: str（波形标识）
baseband: ndarray（原始基带回波数据，维度如 [channels, pulses, samples]）
fc: float（载波频率）
bandwidth: float（信号带宽）
fs: float（采样率）
prf: float（脉冲重复频率）
要求：所有仿真模块，无论内部实现如何，最终必须输出结构完全相同的SimResult对象。
处理结果契约 (ProcessedResult)：
目的：处理层​ 与 可视化层​ 之间的契约。
内容：必须包含所有可视化所需的处理结果与坐标轴信息。一个典型示例包括：
name: str
range_profile: ndarray（距离剖面）
range_doppler: ndarray（距离-多普勒谱）
range_axis: ndarray（距离轴，物理单位如米）
doppler_axis: ndarray（多普勒轴，物理单位如米/秒）
要求：所有处理算法，无论内部逻辑如何，最终必须输出结构完全相同的ProcessedResult对象。
三、五条核心设计原则
单向数据流原则：数据必须严格按照“参数配置 -> 仿真 -> 处理 -> 可视化”的单一方向流动。这是防止代码依赖网混乱的基石。
统一接口契约原则：层与层之间必须通过事先定义好的、统一的数据结构进行通信。消灭临时的、特殊的“胶水代码”和格式转换。
模块独立原则：同层的模块之间应无依赖。例如，FMCW仿真模块的实现不依赖LFM模块的存在。每个模块应可被独立实例化、调用和测试。
无状态处理原则：在算法层和可视化层，尽量使用“纯函数”。即，函数的输出完全由输入参数决定，不依赖或修改任何外部或类的内部状态。这保证了结果的可重现性和可测试性。
渐进式复杂度原则：不要一开始就构建完美、庞大的系统。应从“一个可验证的最小核心”开始（例如，先让FMCW的仿真-处理-可视化完整链路跑通，并物理验证正确），然后在此基础上，遵循上述架构，依次添加新的波形、新的算法。
四、编码与组织实践
注册表模式：在入口层使用一个注册表（如Python字典）来管理可用的仿真模块。新增模块时，仅需实现模块，并在注册表中添加一条映射（如 "sf": stepped_frequency_simulator），系统即可自动识别和调用。
面向契约编程：在编写任何模块时，首先明确其输入和输出契约。函数签名和返回对象应严格遵循契约定义。
分层测试策略：
契约测试：验证SimResult和ProcessedResult的数据结构是否完备、类型是否正确。
模块接口测试：为每个仿真模块、处理算法编写独立的单元测试，验证其输入输出符合契约。
算法验证测试：验证核心处理算法（如距离计算、速度计算）的数学正确性。
物理验证测试：这是置信度测试。设置已知距离、速度的目标场景，验证仿真处理结果是否与物理预期一致。
集成测试：运行从配置到可视化的完整流程，确保各层组合工作正常。
五、快速自查清单（当你准备添加新功能时）
[ ] 我是否在已有层次内工作？（是新增一个仿真模块，还是一个新的处理算法？）
[ ] 我是否明确了我这个模块的输入和输出契约是什么？
[ ] 我的输出是否严格遵循了对应层的统一契约格式？
[ ] 我的实现是否没有引入对同层其他模块的依赖？
[ ] 我是否可以通过修改注册表（而非修改核心代码）来启用我的新功能？
[ ] 我是否为新模块编写了独立的接口测试和物理验证测试？
